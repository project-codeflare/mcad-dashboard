import { Card, CardBody } from '@patternfly/react-core';
import React from 'react';

const MetricTyle = ({ data: num, name: dataName }) => {
  return (
    <Card>
      <CardBody>bro</CardBody>
    </Card>
  );
};

export default MetricTyle;
